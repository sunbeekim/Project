<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.BoardMapper">
    <!-- Get all posts with pagination -->
    <select id="getAllPosts" resultType="com.example.demo.model.Board">
        SELECT id, postId, title, content, forename, createAt, views
        FROM posts
        ORDER BY createAt DESC
    </select>

    <!-- Get post by id -->
    <select id="getPostById" parameterType="Long" resultType="com.example.demo.model.Board">
        SELECT id, postId, title, content, forename, createAt, views
        FROM posts
        WHERE id = #{id}
    </select>

    <!-- Update post -->
    <update id="updatePost" parameterType="com.example.demo.model.Board">
        UPDATE posts
        SET title = #{title},
            content = #{content},
            forename = #{forename}
        WHERE id = #{id} AND postId = #{postId}
    </update>
</mapper>